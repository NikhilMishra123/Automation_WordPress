<h1>Users data </h1>

<table>
  <tr>
    <th>Email </th>
    <th>password </th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.password %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Add New Site', user_sing_in_path %>

